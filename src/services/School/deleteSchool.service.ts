import { SchoolService } from "../../interfaces/school.interface";

interface SchoolInfo {
    id: string;
}

export async function deleteSchoolService(info: SchoolInfo): Promise<SchoolService> {
    return {}
}